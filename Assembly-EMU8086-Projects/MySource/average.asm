DATA SEGMENT
	SUM DB 01 DUP(?)
	AVG DB 01 DUP(?)
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE DS :DATA
 START:
	MOV AX,DATA
	MOV DS,AX
	MOV AX,00
	MOV AL,14H
	ADD AL,A2H
	ADD AL,BCH
	MOV SUM,AL
	MOV BL,03
	DIV BL
	MOV AVG,AL
  CODE ENDS
END START